# Backend API (Voice MoMo)
VITE_API_BASE_URL=http://localhost:3001

# Optional: local AI voice orchestrator (STT+NLU+TTS). When set, the home mic uses recording → port 5004.
# Use your PC LAN IP when testing on a device (same Wi‑Fi).
VITE_VOICE_AI_URL=http://192.168.1.100:5004
